(ns graphite-to-elastic-relay.core-test
  (:require [clojure.test :refer :all]
            [graphite-to-elastic-relay.core :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
